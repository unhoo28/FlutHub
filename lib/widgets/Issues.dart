import 'package:flutter/material.dart';

import 'Empty.dart';

class IssuesWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return EmptyWidget("Issues", true, null);
  }
}
